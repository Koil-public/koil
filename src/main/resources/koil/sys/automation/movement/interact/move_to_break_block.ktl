version: 2
kind: task_template
id: template.movement_interact_move_to_break_block
template_id: movement/interact/move_to_break_block
semantic_operation: sem.task.break_target
params: [target.id, target.selector, radius, stop.distance]
metadata:
  semantic_operations: [sem.task.break_target]
  target_kinds: [block]
  required_params: [target.id]
  optional_params: [target.selector, radius, stop.distance]
  tags: [movement, interact, block]
  description: Move To Break Block
  visibility: public
  model_callable: true
  resource_locks: [movement, look, input]
  side_effects: [moves_player, changes_orientation]
  timeout_ticks: 3600
  failure_policy: recover
  recovery_task: movement/recovery/recover_stuck.ktl
steps:
  - type: delegate
    delegate: movement/navigation/move_to_block.ktl target.id=${target.id} target.selector=${target.selector} radius=${radius} stop.distance=${stop.distance}
  - type: run_primitive
    action: cap.interaction.break_block_target
    params: {}
  - type: return
    label: done
